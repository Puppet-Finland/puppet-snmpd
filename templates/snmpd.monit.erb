### THIS FILE IS MANAGED BY PUPPET. ANY MANUAL CHANGES WILL GET OVERWRITTEN.

check process snmpd with pidfile /var/run/snmpd.pid
   start program = "<%= scope.lookupvar('snmpd::params::service_command') %> start"
   stop  program = "<%= scope.lookupvar('snmpd::params::service_command') %> stop"
   alert <%= scope.lookupvar('snmpd::monit::monitor_email') %> with reminder on 480 cycles
